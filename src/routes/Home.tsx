import React, { ReactElement } from 'react';
import AppLayout from '../layout/AppLayout';

const Home = () : ReactElement => (
    <AppLayout testID="Home">
        App Layout
    </AppLayout>
);

export default Home;
